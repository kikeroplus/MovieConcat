# MovieManager 仕様書（Claude Code 用）

MovieChecker（解像度別フォルダ分け）と concat-videos（フォルダ単位の結合）を統合し、
動画の再生・評価・選別機能を加えた Windows 11 向け GUI アプリ。

- 既存コード: `C:\Users\ilove\Desktop\WindowsApp\MovieChecker`、`C:\Users\ilove\Desktop\WindowsApp\concat-videos`
- 既存ロジックと本仕様が食い違う場合は **本仕様を優先** し、差分を報告すること。
- 実装は `docs/PLAN.md` のフェーズ順に進める。フェーズをまたいだ先回り実装はしない。

---

## 1. 技術スタック

| 用途 | 採用 |
|---|---|
| 言語 | Python 3.11 以上 |
| GUI | PySide6 |
| 再生 | python-mpv（libmpv-2.dll をアプリフォルダに配置） |
| 解析・結合 | ffprobe / ffmpeg（PATH 上にあること。起動時に存在チェック） |
| Windows 評価の読み書き | comtypes（shell32 `IPropertyStore` / `PKEY_Rating` を直接呼び出し。理由は 7 章参照） |
| 削除 | send2trash（ごみ箱へ移動。完全削除はしない） |

## 2. ディレクトリ構成

```
MovieManager/
  main.py
  core/
    probe.py      ffprobe 呼び出し・結果キャッシュ・データクラス
    rating.py     Windows 標準評価の読み書き
    state.py      状態ファイル（除外・並び順・移動履歴）
    grouping.py   解像度別フォルダ分け（MovieChecker 由来）
    concat.py     結合・音声正規化・再エンコード（concat-videos 由来）
    fileops.py    ごみ箱・移動・コピー・元に戻す
    worker.py     QThread ワーカー基盤（進捗・キャンセル）
    settings.py   設定の保存・復元（%LOCALAPPDATA%\MovieManager\settings.json）
    envcheck.py   起動時の外部ツール（ffmpeg/ffprobe/libmpv-2.dll）検出
    applog.py     ログ設定（ファイル出力: %LOCALAPPDATA%\MovieManager\app.log）
    appdir.py     アプリの実体があるディレクトリの解決（ソース実行 / PyInstaller exe 両対応）
    thumbnails.py サムネイル生成・キャッシュ（リレー再生のサムネイル一覧用）
  gui/
    main_window.py
    group_tree.py
    video_table.py  QAbstractTableModel + QSortFilterProxyModel
    player.py       mpv 埋め込みウィジェット
    dialogs.py
    log_widget.py       画面下部のログ表示（QtLogHandler で core.applog.logger と接続）
    thumbnail_strip.py  リレー再生中のサムネイル一覧（横スクロール・クリックでスキップ）
  icon.ico          アプリアイコン（EXE・ウィンドウタイトルバー用）
  MovieManager.spec PyInstaller ビルド定義
  docs/PLAN.md
  CLAUDE.md
```

`core/` は GUI に依存させない（PySide6 を import しない。worker.py を除く）。

## 3. 用語

- **ルート**: ユーザーが選択したフォルダ。
- **未分類動画**: ルート直下にある対象動画。
- **グループフォルダ**: フォルダ分けで作られたルート直下のサブフォルダ（例 `1920x1080`）。
- **出力フォルダ**: `ルート\_output`。スキャン・フォルダ分け・結合の対象外。

## 4. 対象ファイル

- 拡張子 `.mp4` `.m4v` `.wmv`（大文字小文字を区別しない）のみ。その他は無視。
- スキャン範囲はルート直下と、グループフォルダ直下のみ（それより深い階層は見ない）。
- `_output` と `.` で始まるフォルダは無視。

## 5. 画面構成

1 ウィンドウ・3 ペイン。

- **ツールバー**: ［フォルダ選択］［再スキャン］［フォルダ分け］［結合実行］［元に戻す］
  ［別フォルダへまとめる］［設定］［リレー再生］＋「最後まで再生したらループ」
  チェックボックス、結合順の選択（ファイル名昇順／ファイル名降順／作成日時昇順／作成日時降順／手動）、
  リレー再生順の選択（結合順と同じ 5 種類）、［ごみ箱へ］
  - ［フォルダ選択］［フォルダ分け］［結合実行］［リレー再生］の 4 つは主要操作として
    青い枠で囲んで目立たせる。
  - ［ごみ箱へ］は誤操作を避けたい破壊的操作のため、一番右に離して配置し、
    黄色い枠で囲んで注意を引く。
- **左: グループツリー**
  - `未分類 (N本)`
  - `1920x1080 (N本)` などグループフォルダごと
  - `出力動画 (N本)`
  - 選択したノードの動画が中央テーブルに表示される
- **中央: 動画テーブル**
  - 列: チェック / ファイル名 / 長さ / 解像度 / 映像コーデック / 音声 / 作成日時 / ★評価 / 状態（除外中など）
  - 上部にフィルタ: すべて / 評価済み(★1以上) / ★N以上(N を選択) / 未評価 / 除外中
  - 選択ボタン: すべて選択 / 解除 / 反転 / 表示中のみ全選択（チェック列を操作）
  - 手動並び順モード時のみ、「上へ」「下へ」ボタンで並べ替え可能
    （行のドラッグ&ドロップではなくボタン方式で実装。QSortFilterProxyModel 越しの
    ドラッグ move はソート・フィルタとの相性が悪く複雑になるため、シンプルさを優先した）
- **右: プレイヤー**（mpv 埋め込み、シークバー、音量、ループ再生チェックボックス[既定 ON]）
  - 動画が映る部分（動画窓）を**左クリック**すると再生 / 一時停止する。通常再生・
    リレー再生のどちらでも効く（`gui/player.py` の `_video_frame` に仕込んだ
    イベントフィルタが共通で処理するため）。
- **下部**: 進捗バー、キャンセルボタン、ログ表示

### 操作対象の決め方
一括操作（評価・削除・除外・移動）は **チェックされた行** を対象とする。
チェックが 1 つもない場合は **テーブルで選択中の行** を対象とする。
（例外: フォルダ分け・結合はチェックなし時「選択中の行」ではなく「全体」が対象。9・10 章参照）

### キー操作
| キー | 動作 |
|---|---|
| 1〜5 | 評価を付与。対象 1 本なら付与後に次の行へ移動して再生。複数なら移動しない |
| 0 | 評価を消す（未評価に戻す） |
| Space | 再生 / 一時停止 |
| ← / → | 5 秒シーク（Shift で 30 秒） |
| ↑ / ↓ | 行移動（移動先を再生） |
| E | 除外フラグの切り替え |
| Delete | ごみ箱へ（確認ダイアログあり） |
| Ctrl+Z | 元に戻す |

### リレー再生モード

中央テーブルに現在表示されている動画（左ツリーの選択＋フィルタを反映した並び）を
先頭から順に自動再生する。

動画の切り替えは Python 側で毎回ファイルを読み込み直すのではなく、**mpv 自身の
プレイリスト機能**（`loadfile ... append` で全曲を積み、mpv 内部で自動的に次へ進める）
に任せている。EOF を検知してから次のファイルを Python で読み込み直す方式は、
検知〜再読み込みの往復にわずかな遅延があり動画切り替え時の瞬間的なブラックアウトが
目立ちやすかったため、mpv 内部で完結させる方式に作り直した（サムネイルクリック／
ホイールでのスキップも `playlist-play-index`、ループも `loop-playlist` プロパティで
mpv 側に任せている）。プレイリストの 1 本目が直前まで通常再生していたファイルと
同じ場合、そのまま作り直すと mpv 内部で再生が継続してしまい状態検知が狂うことが
あるため、`load_playlist()` は組み直す前に一旦明示的に停止する。

- ［リレー再生］ボタンで開始・終了をトグルする（表示中の動画が 0 本なら押せない）。
- 開始すると、中央の動画テーブルを非表示にし、プレイヤーを画面中央に大きく表示する。
  終了すると 3 ペイン構成に戻る。
- **左のグループツリー（フォルダ選択）はリレー中も表示したまま**にする。
  ツリーで別のフォルダ（未分類／グループフォルダ／出力動画）を選ぶと、リレーを
  終了せずにそのフォルダの動画へ対象を切り替え、先頭から再生し直す
  （サムネイル一覧・進捗表示もあわせて更新する）。選んだフォルダに動画が 0 本の場合は
  何もせず、ステータスバーにその旨を表示する（リレーは継続したまま）。
- **再生順**: ツールバーの「リレー再生順」（結合順と同じ 5 種類。設定に保存）に従い、
  表示中の動画を並べ替えて再生する。リレー中に変更すると先頭から作り直す。
- 「最後まで再生したらループ」チェックボックス（既定 OFF）:
  - OFF: 最後の動画まで再生したらリレー再生を自動的に終了する（3 ペインに戻る）。
  - ON: 最後まで再生したら先頭に戻って続ける。
- プレイヤー単体の「ループ再生」チェックボックス（1 本を繰り返す機能）はリレー再生中は
  無効化される（同時に有効だと 1 本目から次へ進めなくなるため）。リレー終了後は
  チェックボックスの状態に応じたループ設定に戻る。
- 動画の切り替えはリレー専用のロジックで行い、中央テーブルの選択状態（再生中に連動する
  通常の再生）は変更しない。リレー終了後、テーブルは元の選択状態のまま表示される。
- **サムネイル一覧（画面下部・横スクロール）**: リレー再生中のみ表示。リレー対象の
  各動画のサムネイル（ffmpeg で動画の 10% 地点付近から抜き出した静止画）をボタンとして
  横に並べる。クリックでその動画へ即座にスキップする。現在再生中のサムネイルはハイライト
  表示し、自動でスクロールして見える位置に持ってくる。
  - サムネイル画像は `core/thumbnails.py` がバックグラウンドスレッドで生成し、
    `%LOCALAPPDATA%\MovieManager\thumbnails\` にパス＋サイズ＋更新日時をキーに
    キャッシュする（2 回目以降は再生成しない）。生成中はまだ画像が無いボタンとして
    表示され、できたものから順にアイコンが反映される。
  - リレーを終了するとサムネイル一覧は非表示になり、ボタンもクリアされる
    （生成中のバックグラウンドスレッドはキャンセルする）。
- **キー・マウス操作**（リレー再生中）:
  - Space: 再生 / 一時停止（フォーカスがサムネイルのボタン等どこにあっても効く。
    アプリ全体にイベントフィルタを仕込んで割り込んでいる）。
  - ← / →: 前の動画 / 次の動画へ移動する（リレー中はシークの代わり。先頭・末尾では
    それ以上動かない＝ループ設定に関わらずクランプ。コンボボックス操作中は除く）。
  - サムネイル一覧の上でマウスホイール: サムネイル一覧を横スクロールする
    （下へ回す = 右へ、上へ回す = 左へ）。
  - 動画窓（映像が映る部分）の上でマウスホイール: 5 秒シークする
    （上へ回す = 巻き戻し、下へ回す = 早送り）。
  - Delete、またはツールバーの［ごみ箱へ］: 今再生中の動画をごみ箱へ送る（確認ダイアログ
    あり）。中央テーブルが非表示で通常のチェック/選択行を対象にできないため、対象は常に
    「今再生中の動画」になる。削除した動画は詰めて、再生位置はそのまま「次の動画」になる
    （末尾を削除した場合はループ ON なら先頭へ、OFF ならそのままリレー終了）。
    実装は mpv の `playlist-remove` で現在項目だけを取り除く方式ではなく、「次の動画」
    から始まるリストを作って `load_playlist()`（stop → ファイルハンドルの解放待ち →
    再構築、というリレー開始時と同じ処理）で組み直す方式にしている。ループ ON なら
    「次の動画」以降 → 先頭の順で並べ替えたリスト（無限ループなので位相がずれるだけ
    で再生順自体は変わらない）、OFF なら「次の動画」以降だけのリスト（すでに見た分は
    再度読み込まない）を渡す
    （`playlist-remove` 方式は内部的に一旦停止 → 次項目再生という遷移で
    playlist-pos が一時的に不定値を経由することがあり、通常の EOF 検知を誤って
    発火させリレーがそのまま終了してしまう不具合があったため採用していない。
    また `load_playlist()` 直後に `jump_to_playlist_index()` で目的の位置まで
    送る方式も、再構築直後は mpv 側のプレイリストがまだ完全に組み上がっていない
    タイミングがあるらしく、意図しない位置から再生が始まりキー操作を受け付けなく
    なる不具合があったため採用していない）。
    ごみ箱送り自体（send2trash）は他の削除操作と同じくワーカースレッドで行う。
    メインスレッドで直接呼ぶと、シェル操作がメインスレッドのメッセージポンプを
    巻き込み、mpv（ネイティブウィンドウ埋め込み）の入力・描画が一時的に反応しなく
    なる不具合があったため（リレーは中断せず継続するが、キー操作を受け付けなくなり
    リレーの開始/終了で復帰するまで直らなかった）。
    完了後は自動的に再スキャンをかけてテーブル・グループツリーを追随させるが、その
    再スキャンに伴うグループツリーの再選択でリレーが先頭から作り直されないよう
    抑制する（`gui/main_window.py` の `_suppress_relay_resync`）。
- 動画切り替え時の一瞬のブラックアウト対策として、上記の mpv プレイリスト方式への
  作り直しを行った。`keep-open` オプション単体はリレーの自動送り（EOF 検知）を
  破壊したため採用していない。プレイリスト方式でも 1〜2 フレームの黒表示は
  映像出力（vo）の再構成に起因する可能性があり完全な解消は未確認（自動計測では
  検出できない種類の視覚的現象のため、体感の改善は実際の画面での確認が必要）。

## 6. 動画情報（core/probe.py）

ffprobe で以下を取得し、データクラス `VideoInfo` に保持する。

- パス、ファイルサイズ、更新日時
- 表示解像度（幅×高さ）。**回転メタデータ（rotate / displaymatrix）が 90°/270° なら幅と高さを入れ替える**
- 映像: codec_name、pix_fmt、フレームレート（r_frame_rate）、profile
- 音声: codec_name、sample_rate、channels（音声なしも許容）
- 長さ
- 作成日時: `format.tags.creation_time` を優先。無ければ更新日時で代用し、どちらを使ったかをフラグで保持（テーブルに表示）

結果はパス＋サイズ＋更新日時をキーにキャッシュし（`ルート\.moviemanager_cache.json`）、再スキャンを高速化する。
スキャンはワーカースレッドで行い、テーブルは取得できた行から順次埋める。

## 7. 評価（core/rating.py）

- Windows 標準の評価（`System.Rating`）に直接読み書きする。状態ファイルには保存しない。
- 実装は `comtypes` で `shell32.dll` の `SHGetPropertyStoreFromParsingName` を直接呼び出し、`IPropertyStore.GetValue`/`SetValue`/`Commit` を使う。
  **pywin32 の `win32comext.propsys`（`SHGetPropertyStoreFromParsingName` 等 `IPropertyStore` 系を返す関数全般）は、この環境では pip 版 pywin32（312 系・308 系の両方で確認）で
  「登録されたインターフェースが見つからない」エラーが常に発生し使用不可だったため採用しなかった（Phase 1 で実機確認・切り分け済み）。**
  `IPropertyStore` を読み取り専用で開いた直後は明示的に解放してから書き込み用に開き直すこと（開いたままだと書き込みオープンがハングする、または直後の再読み込みが `PermissionError` になる）。
- 書き込み値: ★1=1, ★2=25, ★3=50, ★4=75, ★5=99、評価を消す=プロパティ削除（または 0）
- 読み取り時の変換: 0=未評価, 1-12=★1, 13-37=★2, 38-62=★3, 63-87=★4, 88-99=★5
- **再生中のファイルには書き込めない**。1 本評価時は「次の動画へ切り替え → 前のファイルへ書き込み」の順で行う。
  書き込み対象が再生中なら、先にプレイヤーをアンロードする。
- 書き込みに失敗したファイルはログに出し、評価表示を元に戻す。
- 書き込みで更新日時が変わるのは許容する（作成日時は creation_time を使うため影響しない）。
- Phase 1 で MP4 / M4V / WMV それぞれ書き込みとエクスプローラー上の表示を実機確認すること。

## 8. 状態ファイル（core/state.py）

`ルート\.moviemanager.json`（UTF-8）。パスはすべてルートからの相対パス。

```json
{
  "version": 1,
  "excluded": ["1920x1080/a.mp4", "b.mp4"],
  "manual_order": { "1920x1080": ["x.mp4", "y.mp4"] },
  "history": [ { "op": "move", "items": [["from", "to"]], "time": "..." } ]
}
```

- アプリ内でファイルを移動・改名・削除したときは、excluded / manual_order のパスも更新する。
- ルート外へ移動・削除したファイルは excluded / manual_order から取り除く。
- 保存は一時ファイルへ書いてから置き換える（書き込み途中の破損防止）。

## 9. フォルダ分け（core/grouping.py）

現在のフォルダ配置に関わらず、どのタイミングで「フォルダ分け」を押しても
動画が正しいグループフォルダに収まる状態にする（MovieChecker 同様、繰り返し実行しても安全）。

- 対象: 5 章「操作対象の決め方」のチェック優先ルールに従う。**ただしチェックが 1 つもないときの
  フォールバックは「選択中の行」ではなく「ルート配下全体」**（このボタンの主目的が「いつ押しても
  全体を整理し直す」ことのため、他の一括操作＝評価・削除・除外・移動とはフォールバック先が異なる）。
  - チェックされた行が **1 つでもあれば、それらのみ**が対象（ただしチェック状態は現在表示中のグループの
    テーブルに限定されるため、一度に対象にできるのは今表示しているフォルダ内の動画のみ）。
  - チェックが 1 つもなければ、**未分類・既存のグループフォルダを問わず**ルート配下（`_output` を除く）
    にある動画すべてが対象。
  - いずれの場合も **除外中でないもの** に限る。除外中の動画は元の場所に残す。
- グループ名: `{幅}x{高さ}`。WMV は無劣化結合できないため `{幅}x{高さ}_wmv` として分ける。
- 動画ごとに本来の移動先（`ルート\{グループ名}\ファイル名`）を計算し、**現在地と異なるものだけ**を移動計画に含める。
  既に正しい場所にある動画は対象外（何もしない）。
- 移動先に同名ファイルがある場合はその項目だけスキップし、ログに警告を出す。
  スキップされた動画は元の場所に残る（処理対象にならなかったファイルは放置する）。
- 実行前に「移動予定の一覧（グループごとの本数）」を確認ダイアログで表示する。
- 実行内容は history に 1 操作として記録し、元に戻せるようにする。

## 10. 結合（core/concat.py）

### 対象と順番
- 対象: 5 章「操作対象の決め方」のチェック優先ルールに従う。
  - チェックされた行が **1 つでもあれば、それらのみ**を 1 本のまとめ動画として結合する
    （チェック状態は現在表示中のグループのテーブルに限定されるため、対象は今表示している
    フォルダ内のチェック分のみ）。出力ファイル名には現在表示中のグループ名を使う。
    チェック分の結合は軽い操作として扱うため、判定 C（映像不一致）になった場合は
    再エンコード確認ダイアログを出さず、**「結合できません」のメッセージで終了する**
    （判定 B は通常どおり確認なしで音声正規化して結合する）。
  - チェックが 1 つもなければ、従来どおり **各グループフォルダごとに 1 本ずつ**結合する
    （フォルダ分けと同様、このボタンもチェックなし時のフォールバックは「選択中の行」ではなく
    「グループフォルダ全体」）。
  - いずれの場合も **除外中でない動画**が対象。1 本以下ならスキップ。
- 順番: ツールバーの選択に従う。
  - ファイル名昇順／降順: 自然順ソート（昇順で `2.mp4` < `10.mp4`）
  - 作成日時昇順／降順: 6 章の作成日時
  - 手動: manual_order。未登録のファイルは末尾にファイル名順で追加
- 実行前に確認ダイアログでグループごとの本数・合計時間・判定結果（下記）を表示する。

### 出力
- `ルート\_output\{グループ名}_{実行日YYYYMMDD}.mp4`（WMV グループは `.wmv`）
- 同名ファイルは **上書き**。
- 開始前にプレイヤーを停止・アンロードする（上書き対象のロック回避）。
- 完了後、左ツリーの「出力動画」を更新する。

### 互換性判定と処理方式（再エンコードは極力しない）
グループ内の全ファイルを比較して方式を決める。

| 判定 | 条件 | 処理 |
|---|---|---|
| A: 完全一致 | 映像（codec, 解像度, pix_fmt, fps, profile）と音声（codec, sample_rate, channels）が一致 | concat demuxer + `-c copy` |
| B: 音声のみ不一致 | 映像は一致、音声が不一致または一部音声なし | 各ファイルを一時フォルダへ「映像 `-c:v copy`、音声 AAC 192k / 48kHz / 2ch に変換（音声なしは無音トラックを付与）」してから A と同じ方法で結合 |
| C: 映像不一致 | 映像のいずれかが不一致 | 警告ダイアログで再エンコードするか確認。する場合は全ファイルを共通設定（H.264 / AAC、解像度はグループのもの、fps は最頻値）で変換してから結合。しない場合はそのグループをスキップ |

- B は画質劣化なし・短時間で済むため、確認なしで実行してよい（ログには明記）。
- 一時ファイルは `%TEMP%\MovieManager\` に作り、成功・失敗・キャンセルいずれでも削除する。
- concat リストファイルはパス中の `'` をエスケープし、`-safe 0` を付ける。
- 進捗は ffmpeg の `-progress pipe:1` から算出する。キャンセル時は ffmpeg プロセスを終了し、作りかけの出力を削除する。
- 再エンコード時のエンコーダ（libx264 / NVENC）と品質は設定値として持つ（初期値 libx264, CRF 20）。

## 11. ファイル操作（core/fileops.py）

- **ごみ箱へ**: send2trash。確認ダイアログ必須。元に戻す対象外（ごみ箱から手動で戻す）とダイアログに明記。
- **除外 / 除外解除**: 状態ファイルのフラグのみ変更。ファイルは動かさない。
- **別フォルダへまとめる**: 移動先フォルダを選択し、移動 / コピーを選ぶ。同名ファイルがあればスキップして警告。
  移動は history に記録し元に戻せる。コピーの取り消しは「コピーしたファイルをごみ箱へ」とする。
- **元に戻す**: history の最新操作を逆順に適用。戻し先に既にファイルがある場合はその項目をスキップして警告。
- すべてのファイル操作の前に、対象が再生中ならプレイヤーを停止・アンロードする。

## 12. 共通ルール

- 長時間処理（スキャン・フォルダ分け・結合）はすべてワーカースレッドで行い、UI を止めない。処理中は競合するボタンを無効化する。
- ffmpeg / ffprobe は `subprocess` で `CREATE_NO_WINDOW` を付けて起動する。
- パスは `pathlib.Path` で扱う。日本語・空白を含むパスで必ず動作すること。
- ログは画面下部とファイル（`%LOCALAPPDATA%\MovieManager\app.log`）の両方に出す。
- 設定（最後に開いたフォルダ、結合順、エンコーダ設定、ウィンドウ配置）は `%LOCALAPPDATA%\MovieManager\settings.json`。
- 例外はログに残し、ユーザーには簡潔なダイアログで通知する。アプリを落とさない。
- 作業報告では、実際に実行したコマンドとその出力のみを記載し、実行していない結果を書かない。

## 13. EXE 化（PyInstaller）

- ビルド定義は `MovieManager.spec`（**onefile** 構成。依存関係のない単独 exe 1 個を配布できるように
  ユーザーの要望で onedir から切り替えた）。ビルドコマンド:
  `venv\Scripts\pyinstaller.exe MovieManager.spec --noconfirm`
  出力先は `dist\MovieManager.exe`（1 ファイルのみ、約 90MB）。
- `libmpv-2.dll`（プロジェクトルートに配置したもの）を同梱する。
  ffmpeg / ffprobe は 1 章の方針どおり同梱せず、従来どおり PATH 上にある前提とする
  （`core/envcheck.py` が起動時にチェックする）。
- onefile 実行時、同梱した DLL 等は起動のたびに `%TEMP%` 以下の一時フォルダへ展開される
  （`sys._MEIPASS` が指す場所。exe 本体と同じフォルダではない）。`core/appdir.py` の
  `get_app_dir()` は `sys.frozen` のとき `sys._MEIPASS` を返すようにしているため、
  `libmpv-2.dll` の探索先（`gui/player.py`）と `envcheck.py` のチェック先はこの展開先と
  一致する。ソースから `python main.py` で実行する場合はプロジェクトルートを返す。
- onefile は起動のたびに展開処理が入るため onedir よりわずかに起動が遅いが、配布物が
  exe 1 個で済むことを優先した（ユーザー指定）。
- アイコン（`icon.ico`）は `python-mpv` 等のランタイム依存とは無関係の見た目用アセットで、
  `.spec` の `icon=` と exe に埋め込まれる。生成は Pillow で描画するスクリプト（使い捨て、
  リポジトリには含めない）で作成した。
- `.gitignore` は `build/` `dist/` と `*.spec` を除外するが、`MovieManager.spec` 自体は
  `!MovieManager.spec` で明示的に追跡対象に戻している（ビルド定義はソース扱い）。
